import 'data/array_helper.dart';

void main() {
  var number = [1, 2, 3, 4, 5, 6, 7, 8];
  var name = ['asep', 'budi', 'joko'];

  print(ArrayHelper.count(number));
  print(ArrayHelper.count(name));
}
